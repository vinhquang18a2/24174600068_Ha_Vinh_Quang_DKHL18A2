def pascal_triangle(rows):
    for i in range(rows):
        value = 1 
        for j in range(i + 1):
            print(value, end=" ")
            value = value * (i - j) // (j + 1)
        print() 
rows = int(input("Nhập số hàng của tam giác Pascal: "))
pascal_triangle(rows)
